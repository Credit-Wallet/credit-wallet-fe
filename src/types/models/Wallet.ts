type Wallet = {
  id: string;
  accountId: number;
  balance: number;
  debt: number;
  networkId: number;
  createdAt: string;
  updatedAt: string;
  walletAddress: string;
  balanceOf: number;
};

export type { Wallet };
